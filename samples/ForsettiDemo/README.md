# ForsettiDemo

ForsettiDemo demonstrates how a downstream Windows application composes `ForsettiHostTemplate` with isolated example modules through public framework targets.

The sample registers the example service, UI, and app modules in its composition root. Framework libraries and module targets do not import this sample.
